# 奶龙《空洞骑士》皮肤任务复盘与后续 AI 交接说明

> 文档用途：把本次制作中已经确认的事实、失败点、文件状态和下一次建议完整交给另一位 AI。
>
> 当前结论：本次产物**未通过实机验收**。`nailong` 目录虽然完整，但用户反馈空中、待机和平 A 过程中仍会闪回原版小骑士，因此不能把当前版本当作合格成品。

## 1. 用户明确提出的要求

本次任务中用户实际提出了以下要求：

1. 仔细阅读 `奶龙空洞骑士皮肤_第一阶段计划书.md`，按计划制作奶龙皮肤。
2. 皮肤产物放在 `nailong` 文件夹中。
3. 使用用户提供的奶龙动作参考图替换原先过于诡异的皮肤。
4. 没有修改的文件也要复制到 `nailong`，使该目录可以直接放入游戏使用。
5. 观看 `展示视频.mp4`，根据视频修复：
   - 静止时错误显示向上的动作；
   - 角色会间歇性切换回原版小骑士；
   - 普通攻击也会出现原版小骑士。
6. 分析本次工作造成的 C 盘空间减少，并解决空间问题。

## 2. 计划书和用户请求的关系

`奶龙空洞骑士皮肤_第一阶段计划书.md` 是项目规格和技术约束，不应被误解成用户后来提出的新问题。它规定了：

- 第一阶段重点是玩家本体，不要无目的地修改所有游戏资源；
- `Default/` 是尺寸、坐标、透明区域和动作结构的技术母版；
- 角色应是暖黄色、大圆头、大绿眼、奶油色肚皮、短粗四肢的奶龙，而不是黄色小骑士；
- 必须保留原始动作方向、骨钉方向、攻击判读、sprite sheet 画布尺寸和透明通道；
- 不应整张图集一次性文生图，也不应重排或裁切图集；
- `Knight.png` 是绝对核心，`Sprint.png` 及其他文件需要逐图确认是否有小骑士本体；
- 最终必须做连续动画检查和实机检查。

这次工作虽然读取了计划书，但后续执行没有完全满足“逐帧确认”和“实机检查”这两项关键验收要求。

## 3. 本次使用的输入文件

工作区为：

```text
D:\skin
```

主要输入：

```text
D:\skin\奶龙空洞骑士皮肤_第一阶段计划书.md
D:\skin\展示视频.mp4
D:\skin\Default\
D:\skin\奶龙\奶龙.png
D:\skin\奶龙\奶龙02.png
D:\skin\奶龙\奶龙03.png
D:\skin\斯卡蒂\
D:\skin\小骑士.png
C:\Users\wang\AppData\Local\Temp\codex-clipboard-f26c5ac1-aaa5-43f7-a0b2-e95c774b13ab.png
```

用户提供的参考图是一张约 `1448×1086` 的透明奶龙动作图，包含待机、行走、普通攻击、上下攻击、冲刺、特殊动作和倒地动作等多个分组。

## 4. 本次完成过的工作

### 4.1 初始皮肤目录

- 以 `Default/` 为母版创建了 `nailong/`。
- 复制了 `Default` 的完整资源树。
- 未修改资源保持 Default 原文件内容。
- 当前 `nailong` 包含 Default 的全部 207 个文件，另外有一个 `README.md`，共 208 个文件。
- 根目录 `Default/` 没有被修改。

### 4.2 参考图处理

曾经把参考图做了连通区域分割，得到约 94 个可见组件，并按尺寸、形状和动作外观，把部分奶龙组件合成回目标图集。

参考图中重要的分组大致如下。下面的编号是本次分割脚本产生的组件编号，不是游戏官方动画编号，后续 AI 不能把它们直接当作游戏帧名：

| 组件范围 | 参考图中观察到的内容 |
|---|---|
| `#0–#14` | 正面、侧面、背面等待机/站立姿势 |
| `#15–#28` | 行走/奔跑 |
| `#29–#37` | 普通横向攻击 |
| `#38–#52` | 上劈、下劈及其他竖直攻击 |
| `#53–#63` | 冲刺、翻滚等动作 |
| `#73–#82` | 聚集、空中和特殊动作 |
| `#84–#93` | 倒地/躺下/死亡相关动作 |

高频使用的参考组件位置包括：

```text
正面待机 #2：  (145, 56, 225, 160)
正面待机 #3：  (239, 56, 313, 161)
侧面待机 #13： (1142, 57, 1205, 162)
侧面待机 #14： (1233, 57, 1297, 162)
普通攻击 #29： (687, 306, 842, 421)
普通攻击 #30： (1069, 314, 1231, 423)
空中/特殊 #74：(47, 766, 135, 875)
```

### 4.3 曾经修改的文件

以下九个文件曾被重建或合成：

```text
Knight.png
Sprint.png
DreamArrival.png
Wings.png
Webbed.png
Birthplace.png
Shriek.png
Dreamnail.png
Cloak.png
```

`Hud.png`、`orbicon.png`、NPC、护符、背包、法术、货币、灵魂槽、宠物和场景文件没有被主题化修改，只是从 Default 复制到 `nailong`。

注意：计划书要求对 `Birthplace.png`、`Shriek.png`、`Dreamnail.png`、`Cloak.png` 等文件先人工检查再决定是否修改；本次曾把这些文件列入重建范围，后续 AI 应重新判断这些文件是否真的包含玩家本体，不能沿用这个决定。

### 4.4 视频观察结果

`展示视频.mp4` 时长约 15 秒，分辨率约 `1280×720`。曾以约 100 ms 间隔提取连续画面，并观察到以下现象：

| 时间 | 视频中观察到的现象 |
|---:|---|
| 约 2.987 s | 地面待机短暂出现原版白色小骑士 |
| 约 3.163 s | 黄色角色出现骨钉直指上方的错误姿势 |
| 约 4.963 s | 黄色角色仍处于向上/竖直姿势，像攻击帧而不是自然待机 |
| 约 5.877 s | 黄色角色又出现竖直上挑感的动作 |
| 约 6.773 s | 黄色横向普通攻击和白色斩击特效，看起来相对正常 |
| 约 7.697 s | 空中短暂出现原版小骑士 |
| 约 8.056 s | 出现原版小骑士的向上/斜向攻击画面 |
| 约 8.972 s | 出现原版小骑士的横向普通攻击和白色斩击 |
| 约 9.881 s | 画面边缘再次出现原版小骑士残影/角色块 |
| 约 10.942 s | 空中动作出现原版小骑士 |
| 约 11.473 s | 空中动作仍可能切回原版 |
| 约 12.761 s | 空中/攻击相关动作出现原版小骑士 |
| 约 13.128 s | 地面正面待机清楚出现原版白色小骑士 |

这些时间点是给下一位 AI 做逐帧比对的线索，不是新的游戏动画编号。

## 5. 本次诊断得到的重要技术证据

### 5.1 `Knight.png` 的关键事实

```text
Default/Knight.png       4096×4096，RGBA
nailong/Knight.png       4096×4096，RGBA
```

对 Default 和当前 `nailong/Knight.png` 做像素差异检查时发现：

- 图集左侧约 `x < 2304` 的区域存在大量完全没有被替换的原版小骑士组件；
- 当前皮肤的主要变化集中在 `x >= 2304` 的区域；
- 这解释了视频中为何会在某些动作、空中状态和普通攻击中突然切回 Default；
- 但这只是“图集存在残留”的证据，不足以证明所有视频帧都只来自 `Knight.png`，也不足以证明仅修复这些坐标就能解决实机问题。

曾找到几个与视频中原版角色外观高度相似的 Default 图集源区域：

```text
正面地面待机候选： (2156, 3452, 2250, 3578)
空中/翻转候选：    (2218, 3332, 2320, 3450)
侧面待机候选：     (2252, 3464, 2344, 3590)
```

这些坐标是静态图像比对线索，不能代替对 CustomKnight 实际动画取帧逻辑的确认。

### 5.2 曾经采用的修复方法

曾对 `nailong/Knight.png` 做过一次定点修复：

- 自动把 77 个判断为“左侧残留角色”的图块，匹配到右侧已经变成奶龙的近似图块；
- 另外强制把 12 个候选单元指定为正面待机、侧面待机或空中参考组件；
- 采用保持宽高比、底部对齐的方式贴回目标框；
- 尽量保留白色斩击和其他特效；
- 更新了 `nailong/README.md`；
- 重新生成了 `nailong.zip`。

### 5.3 为什么这个修复仍然失败

用户实机反馈表明，上述静态修复没有解决根本问题。下一位 AI 必须把这一点当作结论，而不是继续在当前方法上盲目增加坐标。

最可能的原因如下：

1. **没有确认 CustomKnight 的真实取帧和加载顺序。**
   - 当前方法默认视频中的所有角色帧都直接来自 `Knight.png`。
   - 实际上空中、攻击、梦境或特殊状态可能来自 `Sprint.png`、`Wings.png`、`Cloak.png`、`DreamArrival.png` 或其他 atlas。
   - 如果实机在读其他文件，继续修改 `Knight.png` 不会消除闪回。

2. **把“图像形状相似”误当成“游戏动画帧相同”。**
   - 自动匹配只比较了连通区域、透明轮廓、宽高比和颜色特征。
   - 相似轮廓可能属于不同动作，例如待机、空中、上劈或受击。
   - 这会导致待机被填入骨钉向上的攻击姿势，正是视频中已经出现的错误。

3. **没有建立游戏动画名/帧索引到 atlas 坐标的权威映射。**
   - `Default/Knight.png` 中有大量重复或相近的角色块。
   - 连通组件的 bbox 也可能只代表身体、武器、斗篷或特效的一部分。
   - 仅按组件 bbox 回填，无法保证同一动画的 pivot、基准线、层级和相邻特效都对应。

4. **修复前没有做完整实机回归。**
   - 曾做过静态图像和视频截图比对，但没有在实际游戏中重新录制修复后的完整视频。
   - 因此无法确认皮肤是否被正确加载、是否有资源缓存、是否有另一份同名皮肤覆盖、是否在不同状态切换到其他图集。

5. **只验证了“候选白色角色区域已变黄”，没有验证“视频使用的每一帧已变成正确奶龙动作”。**
   - 颜色变黄不等于动作正确。
   - 也不等于没有另一个未修改的 atlas 被游戏使用。

6. **当前版本可能把本应保持原样的非角色效果和角色本体混在一起处理。**
   - 一些白色/蓝色区域是聚集光、梦境光、残影或斩击特效，不应该被简单替换成奶龙身体。
   - 下一次必须把角色本体、骨钉、斗篷、残影和特效分层确认。

## 6. 当前工作区文件状态

当前保留的主要文件：

```text
D:\skin\Default\                         # 原始母版，未修改
D:\skin\nailong\                         # 当前失败的中间皮肤版本
D:\skin\nailong.zip                      # 与当前 nailong 同步，但同样未通过实机验收
D:\skin\奶龙\                            # 奶龙参考素材
D:\skin\斯卡蒂\                          # 其他 CustomKnight 皮肤参考
D:\skin\小骑士.png                        # 原版角色参考
D:\skin\展示视频.mp4                      # 用户提供的效果视频
D:\skin\奶龙空洞骑士皮肤_第一阶段计划书.md  # 项目计划书
D:\skin\奶龙皮肤制作任务复盘与AI交接.md    # 本交接文档
```

本次临时诊断图、视频抽帧和分析脚本已经清理，没有把它们混入最终皮肤目录。`Default/` 原文件和用户素材没有删除。

## 7. C 盘空间问题的复盘

本次实际检查到：

- 皮肤图集、视频抽帧和诊断文件主要写在 `D:\skin`，不是 C 盘；
- C 盘用户临时目录曾占用约 `6.04 GB`；
- 其中最大的 `dyqdpzb1` 约 `4.92 GB`，内容是 Visual Studio 更新/组件下载残留；
- 另有 8 个同一批次的临时分卷目录，每个约 `125 MB`，合计约 `0.96 GB`；
- 当时没有发现 Visual Studio 安装器进程在运行；
- 清理了这 9 个已确认的 Visual Studio 临时目录；
- C 盘可用空间从约 `65.51 GB` 增加到约 `71.24 GB`；
- 用户提供的参考图仍保留在 C 盘临时目录；
- 其他仍被占用或无法确认归属的 C 盘临时文件没有强行删除。

因此，C 盘减少的主要原因是 Visual Studio 自身的后台更新残留，不是生成的最终皮肤文件。后续 AI 不要为了制作皮肤而清空整个 C 盘 Temp，也不要删除用户参考图、游戏存档或未知用途的缓存。

## 8. 建议下一位 AI 采用的正确路线

### 第一步：不要把当前 `nailong` 当作可靠母版

建议重新建立一个新目录，例如：

```text
D:\skin\nailong_v2\
```

从 `Default/` 重新复制，而不是继续在当前黄色图集上叠加修补。当前 `nailong/Knight.png` 可能已经包含错误动作映射，继续叠加会让问题更难追踪。

### 第二步：先确认 CustomKnight 的加载逻辑

在实际游戏和 CustomKnight 安装目录中确认：

1. 皮肤目录名和选择方式是否正确；
2. 哪些文件承担待机、跑动、跳跃、空中攻击、普通攻击、上劈和下劈；
3. `Knight.png` 是否被拆成多个 atlas 或由其他文件覆盖；
4. 是否存在同名皮肤、旧压缩包或缓存导致游戏仍加载旧版本；
5. 游戏/模组是否需要重启才能重新加载 PNG；
6. 日志中实际加载的是 `nailong_v2` 还是其他目录。

如果能修改测试素材，建议先做一个“明显测试色”版本：只在一个候选 atlas 区域放入非常醒目的临时颜色或标记，在游戏中触发待机、跳跃和攻击，从而确认游戏到底读取了哪个区域。确认取帧来源后，再放入正式奶龙图像。

### 第三步：建立完整帧目录，而不是只做连通组件匹配

对 `Default` 中所有可能包含玩家本体的文件逐一建立记录：

```text
文件名
原图尺寸
角色区域 bbox
角色本体 bbox
武器 bbox
特效 bbox
动作类别
朝向
脚底/旋转 pivot
是否被其他组件遮挡
```

必须从以下文件中逐图人工确认玩家本体，而不是只凭文件名猜测：

```text
Knight.png
Sprint.png
DreamArrival.png
Wings.png
Webbed.png
Birthplace.png
Shriek.png
Dreamnail.png
Cloak.png
Hud.png
orbicon.png
```

第一阶段的重点是完整替换“明确出现的小骑士本体”，不要求把所有特效、NPC、护符和场景都变成奶龙。

### 第四步：按动作类别逐帧制作

推荐顺序：

1. 正面/侧面待机；
2. 左右转向；
3. 行走和奔跑；
4. 起跳、上升、下落和落地；
5. 左右普通攻击；
6. 空中普通攻击；
7. 上劈和下劈；
8. 受击；
9. 聚集/恢复；
10. 冲刺和特殊动作；
11. 倒地/死亡相关本体动作。

每类动作都要把原小骑士帧与参考图动作一一对应。不能因为宽高相似，就把参考图中的“骨钉向上”组件当成待机帧。

### 第五步：保留原帧结构和分层

正确的合成方式应是：

1. 从 Default 原图裁出一个明确的小骑士本体区域；
2. 保留原区域的 canvas、pivot、脚底线和相邻特效；
3. 只替换角色身体像素；
4. 骨钉如果属于原攻击帧则保留并保持方向；
5. 斩击、梦境光、聚集光和残影不能与角色身体混贴；
6. 将奶龙局部按原角色尺寸和基准线回填，不要任意拉伸到目标 bbox；
7. 每个动作批次都使用同一只奶龙母版，确保眼睛、肚皮和体型稳定。

### 第六步：实机回归必须先于“完成”结论

至少重新录制并检查：

- 原地静止 30 秒；
- 左右移动和转向；
- 跳跃、下落和二段跳；
- 左右普通攻击各 20 次；
- 空中普通攻击；
- 上劈和下劈；
- 受击；
- 聚集/恢复；
- 冲刺/梦门相关动作；
- 重新进入场景、重启游戏后再次检查。

视频中不能出现任何原版白色小骑士本体。待机时不能出现骨钉直指上方的攻击姿势。平 A 中角色本体和骨钉方向必须保持奶龙版动作，白色斩击特效可以保留。

## 9. 下一次制作的验收标准

### 9.1 实机效果

- [ ] 静止时始终是奶龙待机，不再出现竖直上挑攻击姿势。
- [ ] 待机、移动、跳跃、空中状态不会闪回白色小骑士。
- [ ] 左右普通攻击不会闪回原版小骑士。
- [ ] 空中普通攻击、上劈、下劈都使用奶龙本体。
- [ ] 连续动画中不会出现一帧突然换回原版角色。
- [ ] 重启游戏和重新选择皮肤后仍然有效。

### 9.2 角色一致性

- [ ] 主要帧都能直接认出是奶龙，而不是黄色小骑士。
- [ ] 大圆头、大绿眼、奶油色肚皮和短粗四肢稳定存在。
- [ ] 不同动作之间眼睛、身体比例和黄色主色不会突然变化。
- [ ] 攻击方向、骨钉方向和动作判读正确。

### 9.3 文件技术检查

- [ ] 所有输出文件名与 Default 完全一致。
- [ ] 所有修改 PNG 的尺寸与对应 Default 完全一致。
- [ ] Alpha 透明通道正常。
- [ ] 没有黑底、白底、自动裁边或 sprite 位置整体移动。
- [ ] `nailong` 包含实际安装所需的完整资源树。
- [ ] 未修改文件与 Default 哈希一致。
- [ ] 压缩包和文件夹内容一致。

## 10. 给下一位 AI 的直接任务提示

可以把下面这段直接交给下一位 AI：

```text
请接手 D:\skin 中的奶龙《空洞骑士》CustomKnight 皮肤制作。

先阅读：
- D:\skin\奶龙皮肤制作任务复盘与AI交接.md
- D:\skin\奶龙空洞骑士皮肤_第一阶段计划书.md

输入素材：
- D:\skin\Default\：唯一技术母版
- D:\skin\奶龙\奶龙.png、奶龙02.png、奶龙03.png：角色造型参考
- D:\skin\展示视频.mp4：失败效果和实际问题证据
- C:\Users\wang\AppData\Local\Temp\codex-clipboard-f26c5ac1-aaa5-43f7-a0b2-e95c774b13ab.png：用户提供的动作参考图
- D:\skin\小骑士.png：原角色辨识参考

当前 nailong 目录是失败的中间版本，不要直接相信其中的 Knight.png 动作映射。
用户反馈：待机、空中和平 A 过程中仍然会闪回原版小骑士；待机还会误用骨钉向上的动作。

请先确认 CustomKnight 实际加载了哪些 PNG、每种状态使用哪个 atlas、是否有缓存或重复皮肤目录。不要只对 Knight.png 做连通组件的近似匹配，也不要把相似宽高的攻击帧当作待机帧。

建议从 Default 重新建立 nailong_v2，逐文件、逐动作、逐帧记录角色本体、骨钉、特效和 pivot，按原坐标回填奶龙。优先修复并实机验证 Knight.png 的待机、移动、跳跃、空中、普通攻击、上劈和下劈；然后检查其他明确含小骑士本体的 atlas。

在没有重新录制实机视频证明 30 秒待机、反复移动/跳跃/攻击都不出现白色小骑士之前，不要宣布完成。
最终仍需把未修改但游戏需要的文件复制到 nailong_v2 中，并保留 Default 不变。
```

## 11. 最重要的失败教训

> 这次失败的核心不是奶龙画得不够像，而是没有先证明“游戏在每个状态下到底从哪个 atlas、哪个坐标取帧”。
>
> 下一次应先完成加载路径和动画帧映射，再做角色替换；静态图像看起来变黄，不能作为实机动画已经修好的证据。

